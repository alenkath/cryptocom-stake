{
  "name": "cryptocom-stake",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ alenkath/cryptocom-stake",
    "create-local": "graph create --node http://localhost:8020/ alenkath/cryptocom-stake",
    "remove-local": "graph remove --node http://localhost:8020/ alenkath/cryptocom-stake",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 alenkath/cryptocom-stake"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
